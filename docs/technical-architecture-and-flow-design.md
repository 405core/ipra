---
title: 技术架构与流程设计
description: 基于需求书整理的目标技术架构、总体流程与模块流程设计文档。
order: 3
---

# 技术架构与流程设计

本文档基于《需求书》整理目标系统的技术架构与流程设计，用于统一项目对整体分层、关键模块、端到端业务闭环和安全审计落点的理解。

## 1. 文档定位与当前仓库现状

当前仓库仍处于前后端骨架阶段，不代表需求书中的完整系统已经实现：

- `apps/frontend` 为 Vue 3 + Vite 前端骨架，当前仅保留基础路由与页面壳层。
- `apps/backend` 为 Go 后端骨架，当前只有最小化启动入口。
- `docs/` 为 VitePress 文档站，承担需求书、人员分工和后续技术资料的沉淀。

因此，本文以下内容描述的是按需求书落地的**目标技术方案**，用于指导后续系统建设与模块拆分，而不是对当前代码现状的功能宣称。

## 2. 目标总体技术架构

目标系统采用“前端交互层 + Go 业务编排层 + Python AI 能力层 + 受控数据层 + 横切安全审计层”的分层架构，既满足现场问询实时性，也满足敏感数据受控和国产化部署要求。

### 2.1 总体技术架构图

```mermaid
flowchart TB
    subgraph Access["现场接入与采集层"]
        officer["检查员工作站 / 触摸终端"]
        scanner["证件扫描 / 手工录入"]
        camera["摄像头视频流"]
        mic["麦克风阵列"]
    end

    subgraph Frontend["前端交互层 · Vue 3 + TypeScript + Vite"]
        console["问询辅助控制台"]
        profileUI["旅客画像展示"]
        promptUI["问询提示与异常高亮"]
        conclusionUI["人工定性与理由录入"]
    end

    subgraph Backend["Go 业务编排层"]
        gateway["API 网关 / 会话鉴权"]
        profileSvc["画像导入与匹配服务"]
        inquirySvc["问询编排服务"]
        archiveSvc["结论归档与审计服务"]
    end

    subgraph AI["Python AI 能力层"]
        asr["ASR 实时转写服务"]
        videoAI["视频特征分析服务"]
        emotionAI["情感计算 / 异常检测"]
        llm["LLM 推理与追问生成"]
    end

    subgraph Data["数据与存储层"]
        pg["PostgreSQL<br/>业务数据 / 判定结果 / 审计索引"]
        redis["Redis<br/>会话态 / 短时缓存 / 轮次控制"]
        objectStore["MinIO / 对象存储<br/>音视频片段 / 证据材料 / 转写文件"]
    end

    subgraph Security["横切安全与审计层"]
        auth["权限控制"]
        dlp["DLP / 禁导出 / 禁复制"]
        watermark["数字水印叠加"]
        audit["全链路审计日志"]
    end

    subgraph Deploy["部署环境层"]
        intranet["内网受控环境"]
        domestic["国产化服务器 / 算力环境"]
    end

    officer --> console
    scanner --> profileUI
    camera --> videoAI
    mic --> asr

    console --> profileUI
    console --> promptUI
    console --> conclusionUI

    profileUI --> gateway
    promptUI --> gateway
    conclusionUI --> gateway

    gateway --> profileSvc
    gateway --> inquirySvc
    gateway --> archiveSvc

    profileSvc --> pg
    profileSvc --> redis
    inquirySvc --> redis
    archiveSvc --> pg
    archiveSvc --> objectStore

    inquirySvc --> asr
    inquirySvc --> videoAI
    inquirySvc --> emotionAI
    inquirySvc --> llm
    asr --> llm
    videoAI --> emotionAI
    emotionAI --> llm

    auth -.-> gateway
    dlp -.-> console
    dlp -.-> objectStore
    watermark -.-> promptUI
    watermark -.-> conclusionUI
    audit -.-> gateway
    audit -.-> archiveSvc
    audit -.-> pg

    intranet --- Frontend
    intranet --- Backend
    intranet --- AI
    domestic --- AI
    domestic --- Data
```

### 2.2 分层说明

#### 前端交互层

- 提供现场问询辅助主界面，承载旅客画像、问询建议、异常提醒与判定录入。
- 对前端所有操作页面强制叠加数字水印。
- 对敏感数据交互施加 DLP 约束，不提供导出、复制和非授权分享能力。

#### Go 业务编排层

- 对外统一提供 API 网关、登录态校验和工作台会话管理。
- 负责画像导入、旅客匹配、问询流程编排、轮次熔断和结论归档。
- 作为前端与 AI 能力层之间的统一编排中枢，避免前端直连模型与底层存储。

#### Python AI 能力层

- ASR 服务负责实时语音识别与文本流输出。
- 视频分析服务负责外貌体态、微表情、视线等特征提取。
- 情感计算服务融合视频特征和文本语义，输出异常分值与关键时刻。
- LLM 服务负责预评估、首轮问询生成、追问策略和多轮闭环引导。

#### 数据与存储层

- PostgreSQL 存放旅客画像导入结果、问询过程结构化数据、判定结果与审计索引。
- Redis 负责临时会话状态、轮次计数、热点画像缓存与短时推理上下文。
- MinIO 或同类对象存储负责音视频片段、转写文件、异常截图与归档材料。

#### 横切安全与审计层

- 审计日志覆盖登录、查询、问询、判定、查看回放等全过程。
- DLP 约束覆盖前端页面、接口响应和存储访问三个层面。
- 水印贯穿前端页面、视频回放与输出内容。
- 权限控制确保角色、岗位、设备与访问范围一致。

## 3. 总体业务流程设计

总体业务流程围绕“画像预判 -> 现场问询 -> 多模态融合 -> 人工定性 -> 归档审计”展开，安全与审计能力全程伴随介入。

### 3.1 总体业务流程图

```mermaid
flowchart TD
    A["导入旅客画像与高风险名单"] --> B["证件匹配并展示旅客画像"]
    B --> C["LLM 预评估并生成首轮问询策略"]
    C --> D["检查员启动现场问询"]
    D --> E["采集视频流与面部 / 视线特征"]
    D --> F["采集语音并执行 ASR 实时转写"]
    E --> G["多模态情感计算与异常检测"]
    F --> G
    G --> H["LLM 生成异常提示与下一轮追问建议"]
    H --> I{"是否达到轮次上限<br/>或已形成结论"}
    I -- "否" --> J["检查员继续发问"]
    J --> E
    I -- "是" --> K["人工定性：隐瞒 / 虚假陈述 / 无异常"]
    K --> L["录入判定理由与补充说明"]
    L --> M["归档画像、转写、异常片段与审计日志"]

    S["权限校验 / 水印 / DLP / 审计日志"] -.-> B
    S -.-> D
    S -.-> H
    S -.-> K
    S -.-> M
```

## 4. 模块级流程设计

### 4.1 旅客基础画像管理模块（2.1）

该模块负责完成离线数据接入、解析入库、风险名单标记和现场证件匹配，是后续大模型预评估的上下文基础。

```mermaid
flowchart TD
    A["检查员上传 Excel / CSV 文件"] --> B["后端校验文件格式与字段完整性"]
    B --> C["解析画像字段与高风险名单标记"]
    C --> D["写入 PostgreSQL"]
    D --> E["建立证件号 / 旅客标识索引"]
    E --> F["前端扫描或输入证件号"]
    F --> G["快速匹配旅客画像"]
    G --> H{"是否命中高风险名单"}
    H -- "是" --> I["前端突出显示风险预警"]
    H -- "否" --> J["展示普通旅客画像"]

    S["审计日志 + 权限校验 + DLP"] -.-> A
    S -.-> D
    S -.-> G
```

### 4.2 视频特征采集模块（2.2.1）

该模块负责现场视频特征提取，并将异常片段与时间点提供给后续情感计算和前端异常高亮。

```mermaid
flowchart TD
    A["摄像头接入视频流"] --> B["视频帧采样与人脸 / 姿态检测"]
    B --> C["提取外貌体态、微表情、视线落点"]
    C --> D["计算时序特征变化与异常分值"]
    D --> E["输出异常时刻、关键帧与特征标签"]
    E --> F["推送给情感计算服务"]
    E --> G["前端高亮异常瞬间"]
    E --> H["关键片段归档到对象存储"]

    S["水印 + 审计日志 + 受控存储"] -.-> A
    S -.-> G
    S -.-> H
```

### 4.3 语音采集与实时转写模块（2.2.2）

该模块负责把现场对话语音持续转写为可供大模型与审计模块消费的文本流。

```mermaid
flowchart TD
    A["麦克风阵列采集对话语音"] --> B["语音降噪 / 分段 / 说话人切分"]
    B --> C["ASR 实时转写"]
    C --> D["生成问答文本流与时间戳"]
    D --> E["推送给 LLM 编排服务"]
    D --> F["推送给情感计算服务"]
    D --> G["保存转写记录与证据文件"]

    S["权限校验 + 审计日志 + DLP"] -.-> A
    S -.-> D
    S -.-> G
```

### 4.4 大模型智能辅助问询模块（2.3）

该模块是核心编排中枢，负责预评估、首轮策略、异常理解、追问生成和轮次熔断控制。

```mermaid
flowchart TD
    A["读取旅客画像与风险标签"] --> B["LLM 预评估真实出境目的风险"]
    B --> C["生成首轮问询问题清单"]
    C --> D["接收语音转写与视频异常特征"]
    D --> E["融合多模态上下文"]
    E --> F["生成下一轮追问建议与提示话术"]
    F --> G{"是否达到总交互轮次上限"}
    G -- "否" --> H["返回前端供检查员继续发问"]
    H --> D
    G -- "是" --> I["触发熔断提示并结束辅助问询"]

    S["权限校验 + 审计日志 + 轮次控制"] -.-> B
    S -.-> F
    S -.-> I
```

### 4.5 人机协同与结论定性模块（2.4）

该模块负责将模型辅助结果收束为可执行的人工业务判断，并形成完整归档闭环。

```mermaid
flowchart TD
    A["检查员查看画像、转写、异常提示与追问记录"] --> B["选择结论：隐瞒 / 虚假陈述 / 无异常"]
    B --> C["输入结构化判定理由"]
    C --> D["提交结论与理由"]
    D --> E["后端校验必填项与权限"]
    E --> F["写入 PostgreSQL 判定结果"]
    F --> G["归档音视频片段、转写文本与异常证据"]
    G --> H["生成完整审计闭环记录"]

    S["水印 + 审计日志 + DLP"] -.-> A
    S -.-> D
    S -.-> H
```

## 5. 横切安全与审计设计

需求书第 3 章中的非功能要求不应孤立看待，而应贯穿所有模块：

- `3.1` 审计日志：对登录、查询、问询、判定、日志查看、回放等操作自动留痕，并为 PostgreSQL 中的审计索引与对象存储中的证据文件建立可追溯关联。
- `3.2` DLP：从前端交互、接口响应和存储访问三个层面禁止非授权导出、复制和数据外流。
- `3.3` 数字水印：在前端页面、异常高亮画面、视频回放界面和任何输出内容中叠加操作员标识与时间戳。
- `3.4` 国产化部署：AI 推理服务、数据库与对象存储部署在受控内网与国产化算力环境中，避免跨境或外网依赖。
- `3.5` 数据来源受控：画像数据、高风险名单与训练数据集均默认由采购方提供，系统侧只负责受控接入、存储和使用。

## 6. 与当前仓库的演进关系

基于当前代码仓库，后续建议按以下方向演进：

1. 在 `apps/frontend` 中落地问询控制台、画像视图、异常高亮区和判定录入页。
2. 将 `apps/backend` 从示例入口扩展为 Go 业务服务，承接画像导入、问询编排、归档审计等核心能力。
3. 新增独立 AI 服务工程，承接 ASR、视频分析、情感计算与 LLM 编排。
4. 引入 PostgreSQL、Redis 与 MinIO 等基础设施，形成受控数据层。
5. 逐步把需求书中的业务条目映射为可交付的服务接口、状态流转和部署单元。

通过上述演进，当前仓库可以从“前后端骨架 + 文档站”逐步过渡为满足需求书目标的智能旅客风险评估与辅助问询系统。
