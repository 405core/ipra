# Samples

Place short local smoke-test videos here.

`create_smoke_video.py` can generate `smoke.mp4` after `opencv-python` is installed:

```powershell
apps\ai-service\.venv\Scripts\python.exe apps\ai-service\app\create_smoke_video.py
```
